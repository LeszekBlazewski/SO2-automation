credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              description: "Secrets for gerrit jenkins user which allows publishing build status to gerrit."
              id: "gerrit-jenkins-user"
              username: "${JENKINS_USERNAME:-jenkins}"
              password: "${JENKINS_PASSWORD:-jenkins}"
              scope: GLOBAL
